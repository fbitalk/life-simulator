# 部署指南

## GitHub + Vercel 部署

### 需要上传的文件

#### ✅ 必须上传的核心文件

```
life-simulator-3.0/
├── index.html                    # 主入口文件（必须）
├── README.md                     # 项目说明（推荐）
├── event-system-guide.md         # 事件系统指南（推荐）
├── .gitignore                    # Git 忽略文件（推荐）
├── CNAME                         # 自定义域名配置（如果使用 GitHub Pages）
│
├── css/
│   └── style.css                 # 样式文件（必须）
│
└── js/
    ├── game.js                   # 游戏核心（必须）
    ├── eventManager.js           # 事件管理器（必须）
    ├── achievements.js           # 成就系统（必须）
    ├── ui-effects.js             # UI 特效（必须）
    │
    ├── modules/
    │   ├── constants.js          # 常量定义（必须）
    │   └── DataManager.js         # 数据管理（必须）
    │
    └── events/                   # 事件数据库（必须）
        ├── startEvents.js
        ├── commonEvents.js
        ├── maleEvents.js
        ├── femaleEvents.js
        ├── ageEvents.js
        ├── redEvents.js
        ├── blackEvents.js
        ├── purpleEvents.js
        ├── pinkEvents.js
        └── goldEvents.js
```

#### ⚠️ 可选文件

```
tools/                            # 开发工具（可选，不影响部署）
└── event_manager/                # Python Flask 事件管理工具
    ├── app.py
    ├── requirements.txt
    ├── utils.py
    └── templates/
```

### ❌ 不需要上传的文件

- `__pycache__/` - Python 缓存文件
- `debug_output.txt` - 调试输出文件
- `.DS_Store` - macOS 系统文件
- `*.log` - 日志文件

## Vercel 部署步骤

### 1. 准备 GitHub 仓库

1. 在 GitHub 创建新仓库
2. 初始化并上传文件：

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 2. 在 Vercel 部署

1. 访问 [Vercel](https://vercel.com)
2. 使用 GitHub 账号登录
3. 点击 "New Project"
4. 选择你的 GitHub 仓库
5. 配置项目：
   - **Framework Preset**: Other
   - **Root Directory**: `./` (项目根目录)
   - **Build Command**: 留空（纯静态项目无需构建）
   - **Output Directory**: `./` (输出目录就是根目录)
6. 点击 "Deploy"

### 3. 自定义域名（可选）

如果使用 `CNAME` 文件配置自定义域名：
- GitHub Pages: 在仓库设置中启用 Pages，并设置自定义域名
- Vercel: 在项目设置中添加自定义域名

## 注意事项

1. **纯静态项目**：这个项目是纯前端的，Vercel 会自动识别并部署
2. **无需构建**：不需要 npm install 或构建步骤
3. **localStorage**：数据存储在浏览器本地，无需后端
4. **CNAME 文件**：如果使用 GitHub Pages，保留 `CNAME` 文件；如果只用 Vercel，可以删除

## 快速检查清单

上传前确认：
- [ ] 所有 `.js` 文件都在 `js/` 目录下
- [ ] `index.html` 在根目录
- [ ] `css/style.css` 存在
- [ ] 所有事件文件都在 `js/events/` 目录下
- [ ] 已创建 `.gitignore` 文件
- [ ] 已排除 `__pycache__` 和调试文件

## 部署后测试

部署成功后，检查：
1. 游戏主界面是否正常显示
2. 能否创建新角色
3. 事件是否正常触发
4. 数据是否正常保存（localStorage）
5. 主题切换是否正常

## 📝 部署后如何更新事件

### 重要说明

**部署后，游戏是静态文件**。要更新事件，需要：

1. **本地运行事件管理器** → 修改事件
2. **提交到 GitHub** → 推送更改  
3. **Vercel 自动部署** → 更新线上版本

### 快速流程

```bash
# 1. 本地运行事件管理器
cd tools/event_manager
pip install -r requirements.txt
python app.py
# 访问 http://localhost:5000 编辑事件

# 2. 修改完成后，提交更改
cd ../..  # 回到项目根目录
git add js/events/
git commit -m "更新事件：描述你的更改"
git push origin main

# 3. Vercel 会自动检测并重新部署（1-2分钟）
```

### 详细说明

请查看 **[EVENT_MANAGER_GUIDE.md](./EVENT_MANAGER_GUIDE.md)** 获取完整的事件管理器使用指南。

**要点**：
- ✅ 事件管理器在本地运行，直接修改 JS 文件
- ✅ 修改后推送到 GitHub，Vercel 自动重新部署
- ⚠️ 无法直接在线上修改事件（静态部署限制）
- 💡 建议：本地开发 → Git 推送 → 自动部署


# 事件管理器使用指南（部署后）

## 📋 概述

部署到 Vercel 后，游戏是**静态文件**，事件数据直接写在 JS 文件中。要修改事件，需要：

1. **本地运行事件管理器** → 修改事件
2. **提交到 GitHub** → 推送更改
3. **Vercel 自动部署** → 更新线上版本

## 🔧 工作流程

### 方案一：本地开发 + Git 推送（推荐）

#### 1. 本地运行事件管理器

```bash
cd tools/event_manager
pip install -r requirements.txt
python app.py
```

访问 `http://localhost:5000` 使用事件管理器。

#### 2. 修改事件

- 在事件管理器中添加/编辑/删除事件
- 修改会自动保存到 `js/events/` 目录下的 JS 文件

#### 3. 提交并推送

```bash
# 在项目根目录
git add js/events/
git commit -m "更新事件：添加新事件XXX"
git push origin main
```

#### 4. Vercel 自动部署

- Vercel 检测到 GitHub 推送后会自动重新部署
- 通常 1-2 分钟内完成

### 方案二：直接编辑文件 + Git 推送

如果不想运行事件管理器，也可以直接编辑 JS 文件：

```bash
# 1. 编辑事件文件
# 使用任何编辑器打开 js/events/xxxEvents.js

# 2. 提交更改
git add js/events/
git commit -m "更新事件"
git push origin main
```

## 🚀 部署事件管理器（可选）

如果你想在线上也能使用事件管理器，可以单独部署它：

### 选项 A：部署到 Vercel（需要调整）

**注意**：Vercel 是静态/Serverless 平台，Flask 应用需要特殊配置。

1. 创建 `vercel.json`：

```json
{
  "version": 2,
  "builds": [
    {
      "src": "tools/event_manager/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "tools/event_manager/app.py"
    }
  ]
}
```

2. 创建 `requirements.txt` 在项目根目录（或使用现有的）

3. 部署到 Vercel

**限制**：Vercel Serverless 函数无法直接修改文件系统，需要配合 GitHub API 或数据库。

### 选项 B：部署到其他平台

- **Heroku**：支持 Flask，可以修改文件（但文件系统是临时的）
- **Railway**：支持 Flask，可以修改文件
- **PythonAnywhere**：完全支持文件系统操作
- **自己的服务器**：完全控制

### 选项 C：使用 GitHub Actions 自动化

创建一个 GitHub Action，当事件文件更新时自动部署：

```yaml
# .github/workflows/deploy.yml
name: Deploy to Vercel
on:
  push:
    branches: [main]
    paths:
      - 'js/events/**'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
```

## 📝 推荐工作流程

### 日常更新事件

1. **本地运行事件管理器**
   ```bash
   cd tools/event_manager
   python app.py
   ```

2. **在浏览器中编辑事件**
   - 访问 `http://localhost:5000`
   - 添加/修改/删除事件

3. **提交更改**
   ```bash
   git add .
   git commit -m "更新事件：描述你的更改"
   git push
   ```

4. **等待 Vercel 自动部署**（1-2分钟）

### 批量更新

如果需要批量修改，可以：
- 使用事件管理器的批量功能
- 或者直接编辑 JS 文件，然后提交

## ⚠️ 注意事项

1. **文件路径**：事件管理器需要访问 `js/events/` 目录，确保路径正确
2. **编码问题**：事件文件可能使用不同编码（UTF-8, UTF-16），管理器会自动处理
3. **备份**：修改前建议先提交到 Git，方便回滚
4. **测试**：修改后先在本地测试，确认无误再推送

## 🔐 安全建议

如果部署事件管理器到线上：

1. **添加身份验证**：修改 `app.py` 添加登录功能
2. **限制访问**：使用 IP 白名单或 VPN
3. **只读模式**：生产环境建议只读，修改通过 Git 流程

## 📚 相关文件

- `tools/event_manager/app.py` - Flask 应用主文件
- `tools/event_manager/utils.py` - 文件解析工具
- `tools/event_manager/templates/` - HTML 模板
- `js/events/` - 事件数据文件

